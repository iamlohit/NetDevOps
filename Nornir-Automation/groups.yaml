---

cisco_north:
    platform: "ios"
    connection_options:
        scrapli:
            extras:
                ssh_config_file: True
                auth_strict_key: False
        

cisco_south:
    platform: "ios"
    connection_options:
        scrapli:
            extras:
                ssh_config_file: True
                auth_strict_key: False